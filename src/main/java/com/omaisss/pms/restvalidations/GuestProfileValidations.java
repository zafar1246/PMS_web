package com.omaisss.pms.restvalidations;
/*
 * package com.omaisss.pms.validations;
 * 
 * import java.util.regex.Matcher; import java.util.regex.Pattern;
 * 
 * public class GuestProfileValidations {
 * 
 * public static void main(String[] args) {
 * 
 * }
 * 
 * public static boolean isValidFirst_Name(String name) { String regex =
 * "^[A-Za-z]\\s \\w{5,29}$";
 * 
 * // Compile the ReGex Pattern p = Pattern.compile(regex);
 * 
 * // If the username is empty, return false if (name == null) { return false; }
 * 
 * // Pattern class contains matcher() method to find matching between given //
 * username and regular expression.
 * 
 * Matcher m = p.matcher(name);
 * 
 * // Return if the username // matched the ReGex return m.matches(); } }
 */